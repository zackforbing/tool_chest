<h1>Tools Inventory</h1>

<%= link_to 'Create New Tool', new_tool_path %>

<% @tools.each do |tool| %>
  <h3><%= link_to tool.name, tool_path(tool) %></h3>
  <%= "Quantity: #{tool.quantity}" %>
  <br>
  <%= "Price: #{tool.price}" %>
  <br>
  <br>
  <% end %>
